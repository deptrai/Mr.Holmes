# Mr.Holmes — Development Dependencies
# Install: pip install -r requirements-dev.txt
# (Inherits all production dependencies automatically)

-r requirements.txt

# === Testing Framework ===
pytest>=8.0
pytest-asyncio>=0.24,<0.25

# === Async HTTP Mocking ===
# Migrated from aioresponses to unittest.mock-based approach because
# aioresponses is incompatible with aiohttp 3.11+ (ClientResponse.__init__
# requires stream_writer argument). aioresponses kept as optional dev dep
# for any future compatibility fix.
aioresponses>=0.7.7

# === E2E (CLI flow) ===
pexpect>=4.9.0

# === CI/CD & Code Quality ===
flake8>=7.0.0
pytest-cov>=4.1.0
