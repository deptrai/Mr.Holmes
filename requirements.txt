# Mr.Holmes — Production Dependencies
# Install: pip install -r requirements.txt

# === Legacy (existing) ===
PyQRCode==1.2.1
phonenumbers==8.12.25
requests>=2.31.0
pdfkit==1.0.0
beautifulsoup4==4.9.3
stem>=1.4.0

# === Async Engine (Epic 2) ===
aiohttp>=3.9.0
aiofiles>=23.0

# === Configuration & Secrets (Story 4.2 prep) ===
python-dotenv>=1.0.0

# === CLI Modernization (Story 5.3 prep) ===
rich>=13.0

# === Advanced Web Scraping (Epic 6) ===
apify-client>=1.8.0,<2.0.0

# === PDF Export (Story 6.4) ===
jinja2>=3.1.0
# weasyprint>=60.0  # optional — install for PDF generation: pip install weasyprint

# === Web Search Fallback (Epic 8 — SearxNG DDG fallback) ===
duckduckgo-search>=7.0

# === Entity Resolution (Story 9.10) — optional deps, graceful skip if missing ===
jellyfish>=0.11.0   # Jaro-Winkler name similarity (optional)
imagehash>=4.3.0    # Perceptual hash for avatar dedup (optional, requires Pillow)
Pillow>=10.0.0      # Required by imagehash (optional)
